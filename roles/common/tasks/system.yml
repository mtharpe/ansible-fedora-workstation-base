---
- name: Enable net.ipv4.ip_forward
  ansible.posix.sysctl:
    name: net.ipv4.ip_forward
    value: "1"
    sysctl_file: /etc/sysctl.d/99-ansible.conf
    reload: true

- name: Set fs.inotify.max_user_watches to 524288
  ansible.posix.sysctl:
    name: fs.inotify.max_user_watches
    value: "524288"
    sysctl_file: /etc/sysctl.d/99-ansible.conf
    reload: true
  when: install_vscode | default(false)

- name: Allow wheel group to sudo without a password
  ansible.builtin.lineinfile:
    dest: /etc/sudoers
    state: present
    regexp: ^%wheel
    line: "%wheel ALL=(ALL) NOPASSWD: ALL"
    validate: visudo -cf %s

# Firewalld: install and configure a sensible desktop firewall
# Skipped in containers — firewalld requires netfilter access not available in unprivileged containers.
- name: Configure firewalld
  when: not is_container | default(false)
  block:
    - name: Ensure firewalld is installed
      ansible.builtin.dnf:
        name: firewalld
        state: present
      become: true

    - name: Ensure firewalld is enabled and running
      ansible.builtin.service:
        name: firewalld
        enabled: true
        state: started
      become: true

    - name: Get current default firewalld zone
      ansible.builtin.command:
        cmd: firewall-cmd --get-default-zone
      register: common_fw_default
      changed_when: false
      failed_when: false
      become: true

    - name: Set default firewalld zone if different
      ansible.builtin.command:
        cmd: firewall-cmd --set-default-zone={{ firewalld_default_zone | default('public') }}
      when: common_fw_default.stdout is defined and common_fw_default.stdout != (firewalld_default_zone | default('public'))
      changed_when: common_fw_default.stdout is defined and common_fw_default.stdout != (firewalld_default_zone | default('public'))
      become: true

    - name: Ensure common desktop services are allowed
      ansible.posix.firewalld:
        zone: "{{ firewalld_default_zone | default('public') }}"
        service: "{{ item }}"
        permanent: true
        immediate: true
        state: enabled
      loop: "{{ firewalld_allowed_services | default(['ssh', 'mdns', 'dhcpv6-client']) }}"
      become: true

    - name: Ensure configured ports are allowed
      ansible.posix.firewalld:
        zone: "{{ firewalld_default_zone | default('public') }}"
        port: "{{ item }}"
        permanent: true
        immediate: true
        state: enabled
      loop: "{{ firewalld_allowed_ports | default([]) }}"
      when: firewalld_allowed_ports is defined and firewalld_allowed_ports | length > 0
      become: true

# Recover the Intel Wi-Fi/BT combo chip after resume. The btintel_pcie
# controller can wedge across suspend — Powered:no, hci_reset times out — and
# only a driver unbind/rebind clears it. Skipped in containers (no PCI, no
# suspend cycle). Fedora doesn't ship /etc/systemd/system-sleep by default, so
# create it before dropping the hook.
- name: Ensure systemd-sleep hook directory exists
  ansible.builtin.file:
    path: /etc/systemd/system-sleep
    state: directory
    owner: root
    group: root
    mode: "0755"
  become: true
  when: not is_container | default(false)

- name: Install btintel_pcie post-suspend rebind hook
  ansible.builtin.copy:
    src: templates/50-btintel-pcie-rebind
    dest: /etc/systemd/system-sleep/50-btintel-pcie-rebind
    owner: root
    group: root
    mode: "0755"
  become: true
  when: not is_container | default(false)

# Configure git user settings
- name: Ensure .gitconfig exists in user home directory
  ansible.builtin.file:
    path: "/home/{{ local_user }}/.gitconfig"
    state: touch
    owner: "{{ local_user }}"
    group: "{{ local_user }}"
    mode: '0644'
    modification_time: preserve
    access_time: preserve
  become: true

- name: Configure git user.name
  community.general.git_config:
    scope: global
    name: user.name
    value: "Michael Tharpe"
  become: true
  become_user: "{{ local_user }}"

- name: Configure git user.email
  community.general.git_config:
    scope: global
    name: user.email
    value: "mtharpe@users.noreply.github.com"
  become: true
  become_user: "{{ local_user }}"
